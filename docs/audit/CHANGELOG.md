# Revamp changelog — the overnight run of 8–9 Sep 2026

Branch `revamp-trial` only. Nothing touched `main`, the live site, or the database.
Starting point before this run: commit `8de570f`. Every step below is one commit;
to undo a step, `git revert <sha>` on revamp-trial and push.

| Step | Commit | What changed | How to verify on the trial site |
|---|---|---|---|
| Step 1 — Trial site safe (audit P0) | see `git log` "Step 1" | `lib/demo-mode.ts` now allows only the 23 named read RPCs and blocks every other `rpc()` (F-001 — 31 browser-side writers were reaching the live DB). Service-role GETs outside `/api/cron/` refuse on the trial (F-003 Zoho callback, F-005 backup + in4-followup). Un-paged reads of growing tables paged (F-002 CT-wise certificates — Raj Uphaar's 1,987 rows; F-013 JMR). One demo expression (F-011). | Any Cost Control approval → Approve → the "trial site" message, and `cc_approvals` unchanged. Raj Uphaar → Budget → CT wise → Certified rises to `sum(certified_amt)` for project 8. |
| Step 2 — UX quick wins | see `git log` "Step 2" | UX items 2 (project filter in the sidebar), 10 (pills: By category · By order · By CT), 13 (one dash + legend), 15 (one % used colour rule + legend), 21 (SC Budgets Save shows the trial message instead of a 403), 32 (Not-linked states offer "Link it in Masters → Mapping" to reviewers), 33 (the gear says "Setup"), 36 (loading skeletons for the workspace and Masters), 41 (no text under 12 px in the workspace; 44 px chevrons and Print on phones). Item 7 (deep-link to a category) deferred — needs the approval-card link format checked first. | Sidebar: type "ngh" → the tree narrows. Open any project → skeleton for a moment, then Budget pills read By category / By order / By CT. SC Budgets on the trial: the Save button is a message with a link to live. |
| Step 3 — Contractor ledger per WO | see `git log` "Step 3" | Every work order row in the WO/PO tree has a **Ledger** chevron beside Print: all bills and advances in date order with gross, certified, TDS, retention, advance recovered, paid and the running *still to pay*; cancelled/rejected greyed and excluded; footer ties to the order row's Balance, and says so when IN4's header and its bills disagree. **Print ledger** opens `/api/in4/work-order/<id>/ledger`, an A4-landscape page. Desktop table, mobile cards. | SRAH → WO/PO wise → `WO/SRJT/SRAH/2025-26/41` → Ledger: five entries ending at Still to pay ₹44,85,101, the same as the row's Balance. |
| Step 4 — PO print + supplier ledger per PO | see `git log` "Step 4" | Every purchase-order row in the WO/PO tree has **Print** and **Ledger** beside it, both live from IN4. Print renders IN4's own PO template 100 "Purchase Order For SRM" (3,461 of IN4's 3,680 PO prints ever; last used today) with its three repeating rows (material lines, delivery schedule, conditions), the GST split from IN4's tax lines (CGST/SGST/IGST), and the total in words; the pre-flight strip names what IN4 does not hold (HSN on 4,034 of 4,041 materials, delivery schedule on all but 17 orders, SRASSK GSTIN, packing/loading/unloading charges, buyer contact, logo). Ledger = receipts (GRNs with running qty/value) + bills and advances placed by GRN with TDS as paid, retention apart and the running still-to-pay; names the PO IN4 booked a bill under when it differs (PO 92/93). New: `lib/in4/po-print.ts`, `lib/in4/amount-in-words.ts`, `lib/revamp/po-ledger.ts`, `/api/in4/purchase-order/[poId]/print` and `/ledger`; `renderTemplate` generalised to N row groups (WO output unchanged, 24 tests still green). Also fixed a type error in `lib/demo-mode.test.ts` left by Step 1. 1,469 tests, build clean. | Project → WO/PO wise → open a category → Purchase orders → **Print** on PO/SRASSK/NGH/2025-26/92 (one line, CGST 6,916.5 + SGST 6,916.5, total 90,683 "Rupees Ninety Thousand Six Hundred Eighty-Three Only"); **Ledger** on the same PO shows GRN 5,800 kg and the 90,683 bill "IN4 booked under PO/SRASSK/NGH/2025-26/93", still to pay 0. PO/SRJT/SRAH/2026-27/36 prints IGST 14,400 and freight 5,000. |
| Step 5 — Accounts tab | see `git log` "Step 5" | The Accounts tab (ribbon → Money → Accounts) is now built, reviewer-only. It is the WO/PO tree's own figures regrouped — nothing computed afresh: six totals (Ordered with GST · Billed · Paid · Due now · Retention held · Advances outstanding) and four sections, each collapsed to its total until opened: **Due now** (certified in IN4, not yet paid — IN4's CERTIFIED_OUT_AMT / the mirror's outstanding), **Received, not yet billed** (POs whose GRN landed value exceeds their bills), **Held back** (retention and advances outstanding per order), **By party** (every contractor/supplier with Ordered · Billed · Paid · Due · Retention · Adv. o/s, opening to their orders). Every row links to the order's live IN4 Ledger and Print. A PO listed under two categories counts once; orders IN4 returned no header for are left out and counted. New: `lib/revamp/accounts-data.ts` (pure, 7 tests), `AccountsTab.tsx`; the tree now carries `due` per order (supplier: SUM(CERTIFIED_OUT_AMT) placed by GRN; contractor: Σ outstanding on live certificates). Registries: `tabs.ts`/`workspace.ts` flip `accounts` to built (no pills; one screen). | Open a linked project → Accounts. The six totals must equal the WO/PO tree's totals row (Budget → By order). Open "Due now": each row's Ledger page ends on the same still-to-pay. Non-reviewers do not see the tab. |
| Step 6 — Re-audit (read-only) | see `git log` "Step 6" | No code. Each of the seven fixed findings checked in the code and marked **VERIFIED** with the file:line evidence under it in `02-FINDINGS.md` (F-001, F-002, F-003, F-005, F-007, F-011, F-013); F-007 ticked in `03-BACKLOG.md`; `00-SUMMARY.md` opens with the re-audit block and the new verdict — the trial site is safe to hand to reviewers for reading; what remains open is two RLS policies on the shared database (F-004, F-012 — wait for the main round) and eight S/M code items. | Read `docs/audit/00-SUMMARY.md` top block. |

| Step 7 — Masters as per the mind map | see `git log` "Step 7" | `/masters` rebuilt to Aksha's mind map, six lists in its order, every field sourced before a screen was drawn: **Trust Master** (IN4 company register: name, trust address — from `TBLCOMMONCOMPANY.Address/PrintAddress`, the columns that are actually filled — city/pin/state, e-mail, phone, PAN; GST registrations with their registered address; the trust's projects with each site address; SRASSK's missing GST is stated in words, checked in all 105 GSTIN columns), **Project Master** (36 projects → 123 sub-projects with start/end, IN4 status, paying trust, site address, WO counts, and the CT Hub project each feeds), **Contact Master** (SRMD Team from CT Hub users; Consultants = contractors under IN4's "18 Consultants Cost"; 178 Vendors; Contractors — PAN, GST, phone, e-mail, city, contact person, categories), **Budget Categories** (92 main → 369 sub with WO counts; inactive hidden until asked), **Item Master** (4,041 materials with type, sub-type, unit, HSN, IN4 rate), **BOQ Master** (IN4 keeps none — every BOQ item ever ordered, by category → BOQ name → item, with WOs used in, rate min–max and last ordered; live from IN4). Stores and Name mapping kept, set apart in the nav. New `lib/revamp/masters-in4.ts` (7 tests); `MasterTable` draws at most 500 rows and says so; `lib/revamp/masters.ts` trimmed to Stores. All eight IN4 statements run once against live (0.4–1.7 s each). 1,483 tests, build clean. | `/masters` → the six cards show 4 · 36 · contacts · 461 · 4,041 · 7,286-ish. Trusts: SRET shows "Kangvi Road, Karanjveri, Dharampur", GST 24AAETS8557N1ZQ registered at PO Karanjveri; SRASSK says "No GST registration on record in IN4". BOQ → Civil opens 217 BOQ names. |

| Step 8 — Indents in the Internal Estimate's shape + the full cycle + Atm Head notifications | see `git log` "Step 8" | **Indents tab (Budget ribbon → Indents → "By category")** is now the IE tree, live from IN4: category → sub-category → indent → items, columns Items · PO'd (with GST) · Received · Await PO · Await delivery. On top, **Pending approvals in IN4**: every indent and PO at Submitted/Verify for the project — who, since when, how many days, what for, "waiting for the Atm Head to approve in IN4". Every indent carries the **complete cycle**: Draft → Submitted → Verify → Approved (date, person and remark from IN4's audit trail) → each PO with its own chain and supplier → GRN received (qty and date). Each item says what it waits for next (indent approval · PO to be raised · PO approval · delivery · received in full). The old tracker views stay as the second pill ("Tracker"). **Notifications (built, dormant on the trial):** `/api/cron/in4-approvals` in the dispatcher — polls IN4, tells the Atm Head (the `head` chair on the linked CT Hub project) once per event through the normal e-mail/Telegram channel: indent reached Verify, PO reached Verify, GRN received. Memory in one `app_settings` row; idempotent; `?dry=1` shows what would go out. Three new switches on /admin/notifications. New: `lib/revamp/indents-tree.ts` (10 tests), `lib/in4/approvals-watch.ts` (7 tests), `IndentsTree.tsx`, cron route; all 9 IN4 statements run once against live. 1,500 tests, build clean. | Open NGH → Indents: "Pending approvals" shows IND/SRASSK/NGH/2026-27/151 at Verify, raised by Kantilal Kheni on 8 Sep (Fire Fighting, 4 GI-pipe items) — its cycle strip has Draft ✓ Submitted ✓ Verify ● Approved ○ PO ○ GRN ○. Open Civil → Door & Window Sills → IND …/99: Approved, PO/SRASSK/NGH/2025-26/92 Approved ("Checked & Approved By Akshay Sir"), received 5,800 of 5,800. Notifications fire only after merge to main (the trial cannot write). |

| Step 9 — Masters, developer's-hat review (50 scenarios) | see `git log` "Step 9" | Fifty scenarios in `05-MASTERS-SCENARIOS.md`; 18 fixes shipped: **search across all six masters** (`/masters/search`, box on the landing and in the nav — "is Pidilite a vendor or a contractor?" answered in one place, each hit opens its screen with the search already typed); **search on Projects, Categories and inside a BOQ category** (matches shown open); **Download CSV** on every Masters table (rows as shown); Projects split into *with work orders* and *other IN4 projects* (the trust-named placeholders), **"end date passed"** and **"not linked to a CT Hub project"** tags; Contacts: SRMD Team without contractor logins, full address under the city, **duplicates flagged** (JANAK BHAVSAR #223/#225, UTTARA #162/#163 …), **GSTIN/PAN shape check** ("looks wrong"), quiet dashes instead of red "none in IN4" everywhere, a line saying what each group is; Items: HSN header says "7 have one", quiet dashes; Trusts: projects with work orders first, "no work orders" marked; the shared chevron's tooltip now names what opens (was "item-wise BOQ" on every Masters row); landing says when the mirror last synced. 1,503 tests, build clean. | `/masters` → type "pidilite" in the box → hits under Items and BOQ, each opening pre-searched. Contacts → Contractors: JANAK BHAVSAR shows "duplicate of #225 in IN4" and the "Entered twice" tile counts them. Projects: "Pramod" carries "ReSubmit"; search "NGH" shows New Guest House with its six sub-projects open. |

| Step 10 — One Masters section | see `git log` "Step 10" | `/admin/masters` folded into `/masters`; the old URLs redirect (bookmarks and old e-mails keep working). What the older section had and the new one lacked now lives in `/masters` as a second view or group: **Projects → "CT Hub registry"** (hub projects with their IN4 sub-projects, IN4 area/budget, gaps, one-click "Use IN4 area" for admins), **Budget categories → "CT Hub vs IN4"** (the hub's codes against IN4's — the check the budget sync depends on), **Items → "Warehouse vs IN4"** (Warehouse items IN4 does not know, with the link picker for admins), **Contacts → "In CT Hub only"** (typed names with no IN4 party, link picker), **Stores** now IN4's list with the Warehouse matched onto it (was two CT Hub lists), **Name mapping** is the real editor (aliases from IN4, the budget report, the procurement upload and Zoho) — readable by everyone with cost-control, decided by admins; the old read-only diagnosis page is gone. Every Masters table gained filter chips and an action column. Admin registry, Budget tab and WO/PO tree links re-pointed; `lib/masters.ts` trimmed to the four loaders still used. | Open `/admin/masters/mapping` → lands on `/masters/mapping`. Projects → "CT Hub registry" pill shows area/target gaps; Items → "Warehouse vs IN4" lists the unmatched. |

| Step 11 — Masters for every hat (36 scenarios) | see `git log` "Step 11" | `06-MASTERS-HATS.md`: Engineer, Head and Admin questions, each answered from IN4's own data. New **Rates** (`/masters/rates`): *Material purchase rates* — type a material → every PO line (date, PO, supplier, project, qty, rate, value), last rate, min–max, weighted average, cheapest supplier, Print link per PO; with no search, the 300 materials with the most spend, amber where two or more suppliers were 20 %+ apart (145 such materials — "Two prices for one thing"). *BOQ work rates* — type the work → every work-order BOQ line with contractor, project, date, qty, rate (lowest green, highest amber). New **contact record** (`/masters/contacts/<kind>/<id>`, every Contacts row opens it): WOs or POs, ordered/paid/retention, projects, categories, first → last order, each order with Ledger and Print; suppliers add PO → first-GRN days and received in full/partly/not yet, and their rate against the lowest SRMD paid anyone for the same material. New **Housekeeping** (`/masters/housekeeping`): fourteen checks — duplicates, wrong GST/PAN, CT Hub-only contacts/items/stores, no keeper, unmapped sub-projects, unlinked WOs, no area, no IN4 link, category code drift, projects past end date, inactive parties — each with its count and "fix in IN4 / fix in CT Hub" opening the page. Landing: "What people come here for" — three columns of links by hat. Item rows open purchase history; BOQ items link "who charged what →". All six new IN4 statements run once against live (0.8–2.7 s). 1,506 tests, build clean. | `/masters/rates` → type "cement" → 27 PO lines across materials with last/min/max; the landing list shows amber spreads. Contacts → Vendors → NATUROPROTECT → 29 POs, PO → delivery 34 days avg, Pidilite priced +1% over the lowest paid. Housekeeping shows the counts with their links. |

| Step 12 — Indents revamp (34 scenarios) | see `git log` "Step 12" | `07-INDENTS-SCENARIOS.md`. **Honest cycle**: IN4's own "closed for PO" flag wins over our arithmetic — Mataseal T6 (indented 2,000, PO'd 1,900, store closed the line) now reads "received in full · closed for PO in IN4 at 1,900", not "PO to be raised". **Waiting and late**: every line says what it waits for, since when (from the step in IN4's audit trail: approval → PO, PO approval → delivery) and for how many days; late by the same SLA as the digest (approval or PO 2 days, delivery 7) — amber ring on the cycle step, "— late" on the line, a late count on the indent, a Late KPI. **Chips** on the project tab: All · Waiting for approval · Awaiting PO · Awaiting delivery · Late · Received in full (matching branches open). **`/procurement-tracker` is live from IN4 by default**: every project at once, grouped by IN4's own project (no mapping needed), one pending-approvals list across projects, project pills with open-item counts (busiest first), the same chips; window = last 12 months plus every older indent still waiting for anything (964 indents today). PO and ledger links on every PO line. The upload-based tracker (chase notes, digest source) stays under "Upload-based tracker". One shared row set (`IndentRows.tsx`) for both screens. Module tile text updated. 12 tests on the pure cycle logic. | NGH → Indents → chip **Awaiting PO** shows only lines without a PO, each with "PO to be raised · n days" and "— late" past 2 days. `/procurement-tracker` → pending approvals across projects on top; pick a project pill. |

## Where things stand after the run

- Six commits on `revamp-trial` after `8de570f`; each step is one commit and reverts cleanly with `git revert <sha>`.
- 1,476 tests green, `next build` clean at every step. `main` and the live site were not touched; no database change of any kind.
- **Not seen in a browser behind a login** (the trial needs a Google login the preview cannot do): the Accounts tab and the PO Print/Ledger links on the tree. The PO print itself was rendered from live IN4 for four real orders (PO 92, 93, PO/DO/SRJT/SRAH/2023-24/2 with a delivery schedule and HSN codes, PO/SRJT/SRAH/2026-27/36 with IGST and freight) and viewed as a page; the ledger SQL was run for PO 92 and 93 and gives the figures quoted in the Step 4 row.
- **Needs Aksha:** (1) open the Vercel preview after it redeploys and click through Budget → By order → a PO's Print and Ledger, then Accounts; (2) the two database decisions Q1/Q2 (F-004, F-012) for the main round; (3) IN4 login variables must be on the Vercel *Preview* environment or every IN4 page says so (F-014).
- One flaky failure appeared once in a 1,469-test run during Step 4 and did not repeat in the two full runs after; the failing test was not captured.
